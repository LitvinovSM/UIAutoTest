# Конфигурационный файл для запуска.
#BaseURL - URL на котором тестим
#browserType - тип браузера который надо запускать из enum DriverActions
#implicitlyWaitValue,explicitlyWaitValue,pageLoadTimeout - таймауты которые будут проставлены драйверу
web{
    browserType = CHROME
    launchType = "local"
    implicitlyWaitValue = 10
    explicitlyWaitValue = 10
    pageLoadTimeout = 30
}

